### Step-by-Step Execution:

#### **Instruction Set 1**

1. **List of all nodes mentioned in "edges":**
   - `ON_demand_contribution`
   - `BR_demand_contribution`
   - `GAP_demand_contribution`
   - `demand`

2. **Group by "end node" and collect the names of all the "start nodes":**
   - For `demand`: 
     - Start Nodes: `ON_demand_contribution`, `BR_demand_contribution`, `GAP_demand_contribution`

3. **For each group in 2, analyze the start nodes:**

   - **For `ON_demand_contribution`:**
     - a. Definition: ON
     - b. Expected impact: ON is expected to positively impact demand as it contributes to overall demand.
     - c. Observed impact: ON_demand_contribution increased by +0.2% VS LY.
     - d. Do the Expected and Observed impact the same? Yes
     - e. Link word: driven by

   - **For `BR_demand_contribution`:**
     - a. Definition: BR
     - b. Expected impact: BR is expected to positively impact demand as it contributes to overall demand.
     - c. Observed impact: BR_demand_contribution decreased by -2.3% VS LY.
     - d. Do the Expected and Observed impact the same? No
     - e. Link word: despite

   - **For `GAP_demand_contribution`:**
     - a. Definition: GAP
     - b. Expected impact: GAP is expected to positively impact demand as it contributes to overall demand.
     - c. Observed impact: GAP_demand_contribution decreased by -0.9% VS LY.
     - d. Do the Expected and Observed impact the same? No
     - e. Link word: despite

4. **List all nodes under "nodes" and check for edges:**
   - `conversion`: No incoming or outgoing edge.
   - `demand`: Has incoming edges from `ON_demand_contribution`, `BR_demand_contribution`, and `GAP_demand_contribution`.
   - `ATHL_demand_contribution`: No incoming or outgoing edge.
   - `BR_demand_contribution`: Has an outgoing edge to `demand`.
   - `ON_demand_contribution`: Has an outgoing edge to `demand`.
   - `GAP_demand_contribution`: Has an outgoing edge to `demand`.

5. **List all nodes without an edge along with their change numbers:**
   - `conversion` (+17.3% VS LY)
   - `ATHL_demand_contribution` (+3% VS LY)

---

#### **Instruction Set 2**

**Description:**

Demand at $448.8M (+19.8% VS LY) was driven by ON_demand_contribution (+0.2% VS LY), despite BR_demand_contribution (-2.3% VS LY) and GAP_demand_contribution (-0.9% VS LY). Conversion (+17.3% VS LY) and ATHL_demand_contribution (+3% VS LY).