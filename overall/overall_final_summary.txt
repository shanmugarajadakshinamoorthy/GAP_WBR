### Step-by-Step Execution:

#### **Instruction Set 1**

1. **List of all nodes mentioned in "edges" in the knowledge base:**
   - `BR_demand_contribution`
   - `ON_demand_contribution`
   - `GAP_demand_contribution`
   - `demand`

2. **Group by "end node" and collect the names of all the "Start Nodes":**
   - For `demand`: Start Nodes = [`BR_demand_contribution`, `ON_demand_contribution`, `GAP_demand_contribution`]

3. **For each group in 2, analyze the Start Nodes:**

   - **Start Node: BR_demand_contribution**
     - a. Definition: BR
     - b. Expected impact: A decrease in `BR_demand_contribution` would negatively impact `demand` since it represents a reduction in contribution from BR.
     - c. Observed impact: `BR_demand_contribution` decreased by -1.4% VS LY, which aligns with the decline in `demand`.
     - d. Do the Expected and Observed impact the same? Yes
     - e. Link word: driven by

   - **Start Node: ON_demand_contribution**
     - a. Definition: ON
     - b. Expected impact: An increase in `ON_demand_contribution` would positively impact `demand` since it represents a higher contribution from ON.
     - c. Observed impact: `ON_demand_contribution` increased by +3.5% VS LY, which aligns with the decline in `demand` being offset to some extent.
     - d. Do the Expected and Observed impact the same? Yes
     - e. Link word: driven by

   - **Start Node: GAP_demand_contribution**
     - a. Definition: GAP
     - b. Expected impact: A decrease in `GAP_demand_contribution` would negatively impact `demand` since it represents a reduction in contribution from GAP.
     - c. Observed impact: `GAP_demand_contribution` decreased by -1.5% VS LY, which aligns with the decline in `demand`.
     - d. Do the Expected and Observed impact the same? Yes
     - e. Link word: driven by

4. **List all nodes under "nodes" and check for edges:**
   - `demand`: Has incoming edges from `BR_demand_contribution`, `ON_demand_contribution`, and `GAP_demand_contribution`.
   - `BR_demand_contribution`: Has an outgoing edge to `demand`.
   - `ON_demand_contribution`: Has an outgoing edge to `demand`.
   - `GAP_demand_contribution`: Has an outgoing edge to `demand`.

5. **List all nodes without an edge along with their change numbers:**
   - All nodes have edges, so no nodes are without edges.

---

#### **Instruction Set 2**

**Description:**

Demand at $595.8M (-11% VS LY) was driven by ON_demand_contribution (+3.5% VS LY), BR_demand_contribution (-1.4% VS LY), and GAP_demand_contribution (-1.5% VS LY).